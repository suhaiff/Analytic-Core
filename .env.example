# Supabase Configuration
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# Supabase Project URL
# Find this in Supabase Dashboard > Project Settings > API > Project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase Anon Key (for client-side) OR Service Role Key (for server-side)
# For server-side operations, use the Service Role Key
# Find this in Supabase Dashboard > Project Settings > API > Project API keys
SUPABASE_KEY=your-supabase-api-key-here

# Frontend URL (optional, for CORS configuration)
FRONTEND_URL=http://localhost:3000

# Server Port (optional, defaults to 3001)
PORT=3001

# ============================================
# Migration from Dataverse to Supabase
# ============================================
# 
# If you were previously using Dataverse, you can safely remove:
# - TENANT_ID
# - CLIENT_ID
# - CLIENT_SECRET
# - RESOURCE
# - BASE_URL
# - DATAVERSE_API
#
# The old Dataverse environment variables are no longer needed.

# ============================================
# How to get your Supabase credentials:
# ============================================
#
# 1. Go to https://app.supabase.com
# 2. Select your project (or create a new one)
# 3. Navigate to "Project Settings" (bottom left)
# 4. Go to "API" tab
# 5. Copy the following:
#    - Project URL → SUPABASE_URL
#    - Service Role Key → SUPABASE_KEY
#      (Use Service Role Key for server-side operations)
#
# Required Tables (these will be created automatically or manually):
# - users (id, name, email, password, role, created_at)
# - dashboards (id, user_id, name, data_model, chart_configs, created_at)
# - uploaded_files (id, user_id, original_name, mime_type, file_size, sheet_count, created_at, updated_at)
# - excel_sheets (id, file_id, sheet_name, sheet_index, row_count, column_count, created_at)
# - excel_data (id, sheet_id, row_index, row_data, created_at)
# - file_upload_logs (id, file_id, upload_date, upload_time, file_path, status, error_message, created_at)
# - data_configuration_logs (id, file_name, columns, join_configs, config_date, config_time, created_at)

# ============================================
# Security Best Practices:
# ============================================
#
# ✅ DO:
# - Keep .env file in .gitignore
# - Use different API keys for dev/staging/production
# - Rotate API keys regularly
# - Use Row Level Security (RLS) in Supabase for fine-grained access control
# - Hash passwords before storing (use bcrypt or similar)
#
# ❌ DON'T:
# - Commit .env to git
# - Share API keys in chat/email
# - Use same keys across environments
# - Hardcode secrets in source code
# - Use Anon Key for sensitive server-side operations (use Service Role Key instead)
#
